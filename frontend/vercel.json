{
    "version": 2,
    "routes": [
        {
          "src": "/(.*)",
          "dest": "/dist",
          "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
          "headers": {
            "Access-Control-Allow-Origin": "*"
          }
        }
      ]
}